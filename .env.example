# A aplicacao so guarda o que pode aparecer no navegador. Chave de servico e
# token de terceiro ficam em segredo de Edge Function, nunca aqui.

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=

# Endereco publico do sistema, usado nos links dos e-mails de senha
NEXT_PUBLIC_SITE_URL=http://localhost:3000
